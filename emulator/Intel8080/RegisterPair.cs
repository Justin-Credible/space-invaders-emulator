
namespace JustinCredible.SIEmulator
{
    /**
     * A convenience enumeration for opcodes that operate on a pair of registers.
     */
    public enum RegisterPair
    {
        HL,
        BC,
        DE,
    }
}
